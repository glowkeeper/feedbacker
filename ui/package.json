{
  "name": "feedbacker-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Feedbacker's TypeScript browser core (ui/src/core, ADR 0004); the interface arrives in #19.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "contract": "node scripts/contract.ts",
    "contract:check": "node scripts/contract.ts --check",
    "interop": "node scripts/interop-workspace.ts",
    "check:browser": "node scripts/check-browser.ts"
  },
  "devDependencies": {
    "@types/node": "26.6.3",
    "playwright-core": "1.63.0",
    "typescript": "^7.0.2",
    "vite": "8.3.1",
    "vitest": "5.0.2"
  },
  "dependencies": {
    "@noble/hashes": "2.4.0",
    "zod": "4.6.5"
  }
}

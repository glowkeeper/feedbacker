{
  "name": "feedbacker-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker backend for Feedbacker",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240114.0",
    "@vitest/coverage-v8": "^4.1.5",
    "happy-dom": "^20.9.0",
    "typescript": "^5.3.3",
    "vitest": "^4.1.5",
    "wrangler": "^3.27.0"
  }
}
